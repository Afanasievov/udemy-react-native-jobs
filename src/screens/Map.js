import React from 'react';
import { View } from 'react-native';
import { MapView } from 'expo';

const map = () => (
  <View style={{ flex: 1 }}>
    <MapView style={{ flex: 1 }} />
  </View>
);

export default map;
